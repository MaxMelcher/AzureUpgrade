# Recommendation quality matrix

`recommendations-uksouth-linux.csv` is generated by `npm run qa:recommendations` from the UK South catalog.

Each row is one source VM x Linux x CPU-policy combination and includes the selected recommendation, status, GBP prices, monthly savings, confidence, and explanation. The UTF-8 BOM and quoted fields make the file Excel-friendly.
